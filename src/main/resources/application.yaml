spring:
  application:
    name: customers

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    info:
      enabled: true
  info:
    defaults:
      enabled: true

info:
  app:
    name: customers-service
    description: Customers microservice
    version: 1.0.0

logging:
  level:
    com.ordersystem.customers: INFO